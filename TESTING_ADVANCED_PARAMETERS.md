# 高级参数功能测试指南

## 📋 测试前准备

**开发服务器状态**: ✅ 运行中 (http://localhost:5173/)
**调试日志**: ✅ 已启用

---

## 🧪 测试步骤

### 步骤 1: 基础功能验证

1. **打开浏览器**
   ```
   http://localhost:5173/
   ```

2. **上传测试图片**
   - 点击"上传图片"或拖拽图片到页面
   - 等待图片加载完成

3. **导航到压缩工具**
   - 在侧边栏选择"压缩工具"或 "Compression"

4. **检查 UI 渲染**
   - ✅ 应该看到压缩模式选择（Quality Mode / Target Size Mode）
   - ✅ 应该看到格式选择（JPEG / WebP / PNG）
   - ✅ 应该看到质量滑杆
   - ✅ **重要**: 应该看到"⚙️ Advanced Parameters"按钮

---

### 步骤 2: 测试高级参数面板

#### A. 展开高级参数面板

1. **点击"⚙️ Advanced Parameters"按钮**
   - 按钮应该展开/折叠
   - 箭头图标应该旋转

2. **检查控制台输出**
   - 打开浏览器开发者工具 (F12)
   - 切换到 Console 标签
   - 应该看到：
   ```
   📌 [CompressControls] Advanced parameters disabled
   ```

#### B. 测试 WebP 高级参数

1. **选择 WebP 格式**（如果未选择）

2. **展开高级参数面板**
   - 点击"⚙️ Advanced Parameters"按钮

3. **检查 WebP 选项显示**
   - ✅ "WebP Options"标题
   - ✅ Compression Method 滑杆 (0-6)
   - ✅ Filter Strength 滑杆 (0-100)
   - ✅ Filter Sharpness 滑杆 (0-100)
   - ✅ SNS Strength 滑杆 (0-100)

4. **调整参数值**
   - 移动 Compression Method 滑杆到 6
   - 移动 Filter Strength 滑杆到 80
   - 移动 Filter Sharpness 滑杆到 50
   - 移动 SNS Strength 滑杆到 60

5. **检查控制台日志**
   ```
   🎛️ [CompressControls] WebP Advanced Params: {
     method: 6,
     filter_strength: 80,
     filter_sharpness: 50,
     sns_strength: 60
   }
   ```

6. **点击"应用压缩"按钮**
   - 等待压缩完成
   - 检查控制台日志，应该看到：
   ```
   🚀 [CompressTool] Calling WebP compression with params: {
     method: 6,
     filter_strength: 80,
     filter_sharpness: 50,
     sns_strength: 60
   }
   ```

7. **验证压缩结果**
   - 应该看到成功消息
   - 下载压缩后的图片
   - 验证文件格式为 WebP

#### C. 测试 JPEG 高级参数

1. **切换到 JPEG 格式**

2. **检查 JPEG 选项显示**（高级参数应该已经展开）
   - ✅ "JPEG Options"标题
   - ✅ "Optimize Huffman" 复选框
   - ✅ "Progressive JPEG" 复选框

3. **调整参数值**
   - 勾选"Optimize Huffman"
   - 勾选"Progressive JPEG"

4. **检查控制台日志**
   ```
   🎛️ [CompressControls] JPEG Advanced Params: {
     optimize: true,
     progressive: true
   }
   ```

5. **点击"应用压缩"按钮**
   - 等待压缩完成
   - 检查控制台日志，应该看到：
   ```
   🚀 [CompressTool] Calling JPEG compression with params: {
     optimize: true,
     progressive: true
   }
   ```

6. **验证压缩结果**
   - 应该看到成功消息
   - 下载压缩后的图片
   - 验证文件格式为 JPEG

#### D. 测试 PNG 格式

1. **切换到 PNG 格式**

2. **检查提示信息**
   - 应该看到 "PNG is lossless..." 提示
   - 不应该显示任何高级参数选项

3. **点击"应用压缩"**
   - 验证 PNG 压缩正常工作

---

### 步骤 3: 测试参数状态持久性

1. **展开高级参数**
   - 调整 WebP 参数
   - 切换到 JPEG
   - 调整 JPEG 参数

2. **切换格式**
   - 切换回 WebP
   - 验证之前设置的 WebP 参数保持不变

3. **折叠和展开**
   - 折叠高级参数面板
   - 再次展开
   - 验证参数值保持不变

---

### 步骤 4: 测试高级参数禁用状态

1. **折叠高级参数面板**

2. **检查控制台**
   ```
   📌 [CompressControls] Advanced parameters disabled
   ```

3. **应用压缩**
   - 点击"应用压缩"
   - 检查日志，应该看到：
   ```
   🚀 [CompressTool] Calling WebP compression with params: undefined
   ```

4. **验证压缩仍然正常工作**
   - 压缩应该成功完成
   - 只使用基础质量参数

---

## 📊 预期结果

### 控制台日志示例（完整流程）

```javascript
// 1. 初始加载
📌 [CompressControls] Advanced parameters disabled

// 2. 展开高级参数，选择 WebP
🎛️ [CompressControls] WebP Advanced Params: {
  method: 4,
  filter_strength: 60,
  filter_sharpness: 0,
  sns_strength: 50
}

// 3. 调整参数后（实时更新）
🎛️ [CompressControls] WebP Advanced Params: {
  method: 6,
  filter_strength: 80,
  filter_sharpness: 50,
  sns_strength: 60
}

// 4. 应用压缩
🚀 [CompressTool] Calling WebP compression with params: {
  method: 6,
  filter_strength: 80,
  filter_sharpness: 50,
  sns_strength: 60
}

// 5. Worker 处理
📦 [CompressWorker] Calling WASM compress_webp...
Calling WebP compression...
compress_to_webp_advanced: 3344x2434, quality=80 - params logged, using Canvas fallback
WebP params - method: 6, filter_strength: 80, filter_sharpness: 50, sns_strength: 60
WebP compression success 0
✅ Canvas fallback compressed: 32557184 → 101370 bytes

// 6. 切换到 JPEG
🎛️ [CompressControls] JPEG Advanced Params: {
  optimize: true,
  progressive: true
}

// 7. 应用 JPEG 压缩
🚀 [CompressTool] Calling JPEG compression with params: {
  optimize: true,
  progressive: true
}
```

---

## ✅ 验证清单

### UI 渲染
- [ ] "⚙️ Advanced Parameters"按钮显示
- [ ] 按钮可以点击展开/折叠
- [ ] 箭头图标旋转动画正常
- [ ] WebP 格式显示 4 个滑杆
- [ ] JPEG 格式显示 2 个复选框
- [ ] PNG 格式显示提示信息

### 参数传递
- [ ] 展开高级参数后显示参数日志
- [ ] 折叠高级参数后显示禁用日志
- [ ] 调整参数后实时更新日志
- [ ] 应用压缩时显示参数传递日志

### 功能完整性
- [ ] WebP 压缩成功（使用 Canvas fallback）
- [ ] JPEG 压缩成功
- [ ] PNG 压缩成功
- [ ] 压缩文件可下载
- [ ] 文件格式正确

### 状态管理
- [ ] 切换格式后参数保持
- [ ] 折叠/展开后参数保持
- [ ] 禁用高级参数时压缩正常工作

---

## 🐛 故障排除

### 问题 1: 看不到"⚙️ Advanced Parameters"按钮

**可能原因**:
- 组件未正确加载
- CSS 样式问题

**解决方案**:
1. 刷新浏览器 (Ctrl+R 或 Cmd+R)
2. 清除浏览器缓存
3. 检查控制台是否有错误

### 问题 2: 点击按钮后面板不展开

**可能原因**:
- React 状态未更新
- JavaScript 错误

**解决方案**:
1. 检查控制台是否有错误
2. 验证 `showAdvanced` 状态变化
3. 尝试硬刷新 (Ctrl+Shift+R)

### 问题 3: 调整参数后控制台无日志

**可能原因**:
- useEffect 依赖项未正确设置
- 日志被过滤

**解决方案**:
1. 检查控制台过滤器（确保显示所有级别）
2. 验证 CompressControls.tsx 中的 useEffect
3. 确认参数状态确实改变

### 问题 4: 压缩失败

**可能原因**:
- WASM 模块未加载
- Canvas fallback 失败

**解决方案**:
1. 检查 WASM 加载日志
2. 验证图片格式支持
3. 尝试使用较小的测试图片

---

## 🎯 性能测试（可选）

### 测试不同大小图片

| 图片大小 | 分辨率 | 预期时间 | 预期结果 |
|---------|--------|---------|---------|
| < 1MB   | < 1MP  | < 1s    | 快速压缩 |
| 1-5MB   | 1-5MP  | 1-3s    | 正常压缩 |
| 5-10MB  | 5-10MP | 3-5s    | 稍慢 |
| > 10MB  | > 10MP | 5-10s   | 较慢 |

### 测试不同格式

| 格式 | 基础参数 | 高级参数 | 压缩率 | 质量 |
|------|---------|---------|--------|------|
| JPEG | ✅ | ✅ | 70-90% | 良好 |
| WebP | ✅ | ✅ | 80-95% | 优秀 |
| PNG  | ✅ | ❌ | 0-50% | 完美 |

---

## 📝 测试报告模板

完成测试后，请记录以下信息：

```markdown
### 测试环境
- 浏览器: _________
- 操作系统: _________
- 测试日期: _________

### 测试结果
- [ ] UI 渲染正常
- [ ] 参数传递正确
- [ ] 压缩功能完整
- [ ] 性能符合预期

### 发现的问题
1. _________
2. _________

### 其他反馈
_________
```

---

**最后更新**: 2026-01-11
**测试工具**: Chrome DevTools / Firefox Developer Tools
**日志级别**: All (确保看到所有日志)
